kind: ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: test-ingress
spec:
  rules:
  - http:
      paths:
      - path:
          backend:
            resource:
              name:
            service:
              name: 
              
